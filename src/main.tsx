import React from 'react'
import ReactDOM from 'react-dom/client'
import TarotTraderApp from './App'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TarotTraderApp />
  </React.StrictMode>,
)
